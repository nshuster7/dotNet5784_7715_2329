<Window x:Class="PL.EnterUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Enter User" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Enter Username:" Margin="10" FontWeight="Bold"/>
        <TextBox Grid.Row="0" Margin="117,10,83,10" Name="UserNameTextBox" MinWidth="200"/>

        <TextBlock Grid.Row="1" Text="Enter Password:" Margin="10" FontWeight="Bold"/>
        <PasswordBox Grid.Row="1" Margin="117,10,83,10" Name="PasswordBox" MinWidth="200"/>

        <TextBlock Grid.Row="2" Text="Enter ID Number:" Margin="10" FontWeight="Bold"/>
        <TextBox Grid.Row="2" Margin="117,10,83,10" Name="IdTextBox" MinWidth="200"/>

        <Button Grid.Row="4" Margin="145,85,145,-64" Content="Login"  MinWidth="80"/>
        <Button Grid.Row="4" Margin="145,127,145,-106" Content="Cancel" MinWidth="80"/>
    </Grid>
</Window>



